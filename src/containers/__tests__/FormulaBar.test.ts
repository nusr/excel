// import { FormulaBarContainer } from '..';
// import { render } from '@/react';

// describe('FormulaBarContainer.test.ts', () => {
//   test('normal', () => {
//     const dom = render(document.body, FormulaBarContainer({}));
//     expect(dom.querySelector('.formula-bar-wrapper')!.childNodes).toHaveLength(
//       2,
//     );
//     expect(dom.querySelector('.formula-bar-name')!.textContent).toEqual('A1');
//   });
// });
