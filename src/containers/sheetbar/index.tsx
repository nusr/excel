import React, { memo } from "react";
import { Container } from "@/components/styled";

export const SheetBar = memo(() => {
  return <Container id="sheet-bar-container">SheetBar</Container>;
});

SheetBar.displayName = "SheetBar";
